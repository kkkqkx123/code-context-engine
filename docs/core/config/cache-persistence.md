改方向基本正确。不过需要考虑每次操作时热处理是否有问题，需要考虑如何缓存
是否需要改为全局维护关联的项目列表，持久化则通过sqlite实现(例如rkyv序列化+zstd压缩+blob存储，主键使用项目id)项目的配置。因为当前绑定项目的场景一般都是热更新的情况
对于查询的情况，也可以使用同样的方式，从全局维护的配置实例获取
并提供api用于手动对关联的项目列表(包含配置)执行crud操作，且需要立刻持久化

该设计只是初步设计，可以进一步调整